Ghost has been installed.

For production, configure:
  - env.url to the public HTTPS URL
  - mysql.auth.existingSecret or mysql.auth.password for the MySQL password
  - mail settings for member signup and newsletter email

{{- if .Values.mysql.enabled }}

This release includes a MySQL StatefulSet:

  {{ include "ghost.mysqlServiceName" . }}:{{ .Values.mysql.service.port }}
{{- end }}

To reach Ghost inside the cluster:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "ghost.fullname" . }} 2368:2368

Then open:

  http://127.0.0.1:2368/

Ghost Admin is available at:

  {{ .Values.env.url }}/ghost
