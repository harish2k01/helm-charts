Scrutiny web is available through the scrutiny service on port {{ .Values.service.port }}.

Remote collectors should post to the web/API endpoint, for example:
  scrutiny-collector-metrics run --api-endpoint "https://{{ first .Values.httpRoute.hostnames }}"

For the official collector container, set:
  COLLECTOR_API_ENDPOINT=https://{{ first .Values.httpRoute.hostnames }}
