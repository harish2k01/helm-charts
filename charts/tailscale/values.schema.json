{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "tailscale values",
  "description": "Configurable Helm values for the tailscale chart.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "replicaCount": {
      "type": "integer",
      "description": "Number of application pod replicas to run. Increase for horizontally scalable services when storage and application behavior allow multiple pods.",
      "default": 1
    },
    "image": {
      "type": "object",
      "description": "Container image settings for the main application container.",
      "additionalProperties": true,
      "properties": {
        "repository": {
          "type": "string",
          "description": "Container image repository used for the main application container.",
          "default": "tailscale/tailscale"
        },
        "tag": {
          "type": "string",
          "description": "Container image tag to deploy. Pin this to a concrete version for repeatable installs and predictable upgrades.",
          "default": "v1.96.5"
        },
        "pullPolicy": {
          "type": "string",
          "description": "Kubernetes image pull policy for the container.",
          "default": "IfNotPresent"
        }
      }
    },
    "imagePullSecrets": {
      "type": "array",
      "description": "Optional list of image pull secrets used when the container image is stored in a private registry.",
      "default": [],
      "items": {}
    },
    "nameOverride": {
      "type": "string",
      "description": "Override the chart name portion used when generating Kubernetes resource names.",
      "default": ""
    },
    "fullnameOverride": {
      "type": "string",
      "description": "Override the full generated Kubernetes resource name.",
      "default": ""
    },
    "podAnnotations": {
      "type": "object",
      "description": "Additional annotations applied to application pods.",
      "additionalProperties": true,
      "properties": {}
    },
    "podLabels": {
      "type": "object",
      "description": "Additional labels applied to application pods.",
      "additionalProperties": true,
      "properties": {}
    },
    "podSecurityContext": {
      "type": "object",
      "description": "Pod-level security context applied to all containers in the pod.",
      "additionalProperties": true,
      "properties": {}
    },
    "securityContext": {
      "type": "object",
      "description": "Container-level security context for the main application container.",
      "additionalProperties": true,
      "properties": {
        "privileged": {
          "type": "boolean",
          "description": "Run the container in privileged mode so it can configure networking or access required devices.",
          "default": true
        },
        "capabilities": {
          "type": "object",
          "description": "Configuration block for capabilities.",
          "additionalProperties": true,
          "properties": {
            "add": {
              "type": "array",
              "description": "List of add entries.",
              "default": [
                "NET_ADMIN",
                "NET_RAW"
              ],
              "items": {
                "type": "string",
                "description": "Entry in the add list.",
                "default": "NET_ADMIN"
              }
            }
          }
        }
      }
    },
    "sysctl": {
      "type": "object",
      "description": "Sysctl init container settings used to prepare networking behavior for the pod.",
      "additionalProperties": true,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable or disable this feature or generated resource.",
          "default": true
        },
        "image": {
          "type": "object",
          "description": "Image setting for image.",
          "additionalProperties": true,
          "properties": {
            "repository": {
              "type": "string",
              "description": "Container image repository.",
              "default": "busybox"
            },
            "tag": {
              "type": "string",
              "description": "Container image tag.",
              "default": "1.37"
            },
            "pullPolicy": {
              "type": "string",
              "description": "Kubernetes image pull policy.",
              "default": "IfNotPresent"
            }
          }
        },
        "securityContext": {
          "type": "object",
          "description": "Configuration block for securityContext.",
          "additionalProperties": true,
          "properties": {
            "privileged": {
              "type": "boolean",
              "description": "Run the container in privileged mode so it can configure networking or access required devices.",
              "default": true
            }
          }
        },
        "ipv4Forwarding": {
          "type": "boolean",
          "description": "Enable IPv4 packet forwarding in the pod network namespace.",
          "default": true
        },
        "ipv6Forwarding": {
          "type": "boolean",
          "description": "Enable IPv6 packet forwarding in the pod network namespace.",
          "default": true
        }
      }
    },
    "authKey": {
      "type": "object",
      "description": "Tailscale authentication key Secret settings.",
      "additionalProperties": true,
      "properties": {
        "existingSecret": {
          "type": "string",
          "description": "Existing Kubernetes Secret containing this value.",
          "default": ""
        },
        "existingSecretKey": {
          "type": "string",
          "description": "Key inside the existing Secret that contains the auth key.",
          "default": "TS_AUTHKEY"
        },
        "create": {
          "type": "boolean",
          "description": "Create a Kubernetes Secret from authKey.value.",
          "default": false
        },
        "value": {
          "type": "string",
          "description": "Plain Tailscale auth key used only when authKey.create is true. Avoid committing real keys.",
          "default": ""
        }
      }
    },
    "tailscale": {
      "type": "object",
      "description": "Tailscale sidecar or proxy runtime settings.",
      "additionalProperties": true,
      "properties": {
        "hostname": {
          "type": "string",
          "description": "Hostname advertised to the Tailnet by this Tailscale node.",
          "default": "tailscale-router"
        },
        "hostnameFromPodName": {
          "type": "boolean",
          "description": "Append or derive the advertised Tailscale hostname from the StatefulSet pod name.",
          "default": true
        },
        "hostnamePrefix": {
          "type": "string",
          "description": "Optional prefix used when deriving hostnames from pod names.",
          "default": ""
        },
        "stateDir": {
          "type": "string",
          "description": "Directory where Tailscale stores node state.",
          "default": "/var/lib/tailscale"
        },
        "kubeSecret": {
          "type": "string",
          "description": "Optional Kubernetes Secret name used by the official Tailscale image for state or auth integration.",
          "default": ""
        },
        "userspace": {
          "type": "boolean",
          "description": "Run Tailscale in userspace networking mode instead of kernel TUN mode.",
          "default": false
        },
        "acceptDns": {
          "type": "boolean",
          "description": "Accept DNS settings advertised by the Tailnet.",
          "default": false
        },
        "authOnce": {
          "type": "boolean",
          "description": "Authenticate only when state is missing, preserving the node identity after restarts.",
          "default": true
        },
        "enableHealthCheck": {
          "type": "boolean",
          "description": "Enable the local Tailscale health check endpoint.",
          "default": true
        },
        "enableMetrics": {
          "type": "boolean",
          "description": "Enable the local Tailscale metrics endpoint.",
          "default": true
        },
        "localAddrPort": {
          "type": "string",
          "description": "Local address and port where Tailscale exposes health and metrics endpoints.",
          "default": "[::]:9002"
        },
        "advertiseExitNode": {
          "type": "boolean",
          "description": "Advertise this node as a Tailnet exit node.",
          "default": true
        },
        "routes": {
          "type": "array",
          "description": "List of routes entries.",
          "default": [
            "192.168.0.0/24"
          ],
          "items": {
            "type": "string",
            "description": "Entry in the routes list.",
            "default": "192.168.0.0/24"
          }
        },
        "extraArgs": {
          "type": "array",
          "description": "Additional tailscaled or tailscale CLI arguments rendered by the chart.",
          "default": [],
          "items": {}
        },
        "tailscaledExtraArgs": {
          "type": "array",
          "description": "List of tailscaledExtraArgs entries.",
          "default": [],
          "items": {}
        },
        "extraEnv": {
          "type": "object",
          "description": "Configuration block for extraEnv.",
          "additionalProperties": true,
          "properties": {}
        },
        "envFrom": {
          "type": "array",
          "description": "List of envFrom entries.",
          "default": [],
          "items": {}
        }
      }
    },
    "persistence": {
      "type": "object",
      "description": "Persistent storage configuration used by the chart.",
      "additionalProperties": true,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether to create or mount persistent storage for this component.",
          "default": true
        },
        "mountPath": {
          "type": "string",
          "description": "Path inside the container where the persistent volume is mounted.",
          "default": "/var/lib/tailscale"
        },
        "existingClaim": {
          "type": "string",
          "description": "Name of an existing PersistentVolumeClaim to mount instead of creating a new one.",
          "default": ""
        },
        "accessModes": {
          "type": "array",
          "description": "Access modes requested by the generated PersistentVolumeClaim.",
          "default": [
            "ReadWriteOnce"
          ],
          "items": {
            "type": "string",
            "description": "PersistentVolumeClaim access mode value.",
            "default": "ReadWriteOnce"
          }
        },
        "size": {
          "type": "string",
          "description": "Requested size for the generated PersistentVolumeClaim.",
          "default": "1Gi"
        },
        "storageClassName": {
          "type": "string",
          "description": "StorageClass name for the generated PVC. Leave empty to use the cluster default.",
          "default": ""
        }
      }
    },
    "service": {
      "type": "object",
      "description": "Kubernetes Service configuration for exposing the application inside the cluster.",
      "additionalProperties": true,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether to create the Kubernetes Service for this endpoint.",
          "default": true
        },
        "type": {
          "type": "string",
          "description": "Kubernetes Service type, such as ClusterIP, NodePort, or LoadBalancer.",
          "default": "ClusterIP"
        },
        "port": {
          "type": "integer",
          "description": "Service port exposed inside the cluster.",
          "default": 9002
        },
        "targetPort": {
          "type": [
            "integer",
            "string"
          ],
          "description": "Container port or named port that receives traffic from the Service.",
          "default": 9002
        },
        "portName": {
          "type": "string",
          "description": "Name assigned to the Service port and matching container port where applicable.",
          "default": "http"
        },
        "annotations": {
          "type": "object",
          "description": "Additional annotations applied to the Service resource.",
          "additionalProperties": true,
          "properties": {}
        },
        "extraPorts": {
          "type": "array",
          "description": "Additional Service ports to expose beside the primary application port.",
          "default": [],
          "items": {}
        }
      }
    },
    "tunDevice": {
      "type": "object",
      "description": "Host /dev/net/tun device mount settings required for userspace networking modes that need TUN access.",
      "additionalProperties": true,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable or disable this feature or generated resource.",
          "default": true
        },
        "hostPath": {
          "type": "string",
          "description": "hostPath volume source configuration.",
          "default": "/dev/net/tun"
        },
        "mountPath": {
          "type": "string",
          "description": "Container filesystem path where this volume is mounted.",
          "default": "/dev/net/tun"
        }
      }
    },
    "extraContainerPorts": {
      "type": "array",
      "description": "Additional container ports to add to the main application container.",
      "default": [],
      "items": {}
    },
    "extraVolumes": {
      "type": "array",
      "description": "Additional Kubernetes volumes appended to the pod spec.",
      "default": [],
      "items": {}
    },
    "extraVolumeMounts": {
      "type": "array",
      "description": "Additional volume mounts appended to the main application container.",
      "default": [],
      "items": {}
    },
    "resources": {
      "type": "object",
      "description": "CPU and memory requests and limits for the main application container.",
      "additionalProperties": true,
      "properties": {}
    },
    "livenessProbe": {
      "type": "object",
      "description": "Kubernetes liveness probe used to restart the container when it becomes unhealthy.",
      "additionalProperties": true,
      "properties": {
        "httpGet": {
          "type": "object",
          "description": "HTTP GET probe configuration.",
          "additionalProperties": true,
          "properties": {
            "path": {
              "type": "string",
              "description": "HTTP path match configuration.",
              "default": "/healthz"
            },
            "port": {
              "type": [
                "integer",
                "string"
              ],
              "description": "Port number or named port used by this setting.",
              "default": "http"
            }
          }
        },
        "initialDelaySeconds": {
          "type": "integer",
          "description": "Seconds to wait after container start before the probe first runs.",
          "default": 20
        },
        "periodSeconds": {
          "type": "integer",
          "description": "How often, in seconds, Kubernetes runs the probe.",
          "default": 30
        },
        "timeoutSeconds": {
          "type": "integer",
          "description": "Seconds after which the probe attempt times out.",
          "default": 5
        },
        "failureThreshold": {
          "type": "integer",
          "description": "Number of failed probe attempts before Kubernetes marks the container unhealthy or unready.",
          "default": 3
        }
      }
    },
    "readinessProbe": {
      "type": "object",
      "description": "Kubernetes readiness probe used to decide when the pod can receive traffic.",
      "additionalProperties": true,
      "properties": {
        "httpGet": {
          "type": "object",
          "description": "HTTP GET probe configuration.",
          "additionalProperties": true,
          "properties": {
            "path": {
              "type": "string",
              "description": "HTTP path match configuration.",
              "default": "/healthz"
            },
            "port": {
              "type": [
                "integer",
                "string"
              ],
              "description": "Port number or named port used by this setting.",
              "default": "http"
            }
          }
        },
        "initialDelaySeconds": {
          "type": "integer",
          "description": "Seconds to wait after container start before the probe first runs.",
          "default": 10
        },
        "periodSeconds": {
          "type": "integer",
          "description": "How often, in seconds, Kubernetes runs the probe.",
          "default": 10
        },
        "timeoutSeconds": {
          "type": "integer",
          "description": "Seconds after which the probe attempt times out.",
          "default": 5
        },
        "failureThreshold": {
          "type": "integer",
          "description": "Number of failed probe attempts before Kubernetes marks the container unhealthy or unready.",
          "default": 3
        }
      }
    },
    "nodeSelector": {
      "type": "object",
      "description": "Node selector constraints for scheduling pods onto matching nodes.",
      "additionalProperties": true,
      "properties": {}
    },
    "tolerations": {
      "type": "array",
      "description": "Pod tolerations that allow scheduling onto tainted nodes.",
      "default": [],
      "items": {}
    },
    "affinity": {
      "type": "object",
      "description": "Pod affinity and anti-affinity rules for advanced scheduling control.",
      "additionalProperties": true,
      "properties": {}
    },
    "topologySpreadConstraints": {
      "type": "array",
      "description": "Topology spread constraints used to distribute pods across zones, nodes, or other topology domains.",
      "default": [],
      "items": {}
    }
  }
}
