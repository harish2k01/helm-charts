{{- $builtinPostgreSQL := .Values.database.postgresql.enabled }}
{{- $externalDatabaseEnabled := .Values.database.external.enabled }}
{{- if and $builtinPostgreSQL $externalDatabaseEnabled }}
{{- fail "Enable only one database section: database.postgresql or database.external" }}
{{- end }}
{{- if not (or $builtinPostgreSQL $externalDatabaseEnabled) }}
{{- fail "Umami requires PostgreSQL. Enable database.postgresql or database.external." }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "umami.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "umami.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "umami.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/umami-secrets: {{ toJson .Values.umami.secrets.inline | sha256sum }}
        checksum/umami-database: {{ toJson .Values.database | sha256sum }}
        umami/rollout-token: {{ .Values.umami.secrets.rolloutToken | quote }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "umami.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if $builtinPostgreSQL }}
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z {{ include "umami.fullname" . }}-db {{ .Values.database.postgresql.service.port }}; do echo waiting for PostgreSQL; sleep 2; done;
      {{- end }}
      containers:
        - name: umami
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.umami.env }}
            {{- if not (has $key (list "APP_SECRET" "DATABASE_URL" "DATABASE_PASSWORD")) }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ default (include "umami.fullname" .) .Values.umami.secrets.existingSecret }}
                  key: {{ .Values.umami.secrets.appSecretKey }}
            {{- if $builtinPostgreSQL }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-db" (include "umami.fullname" .)) .Values.database.postgresql.auth.existingSecret }}
                  key: {{ .Values.database.postgresql.auth.passwordKey }}
            - name: DATABASE_URL
              value: "postgresql://{{ .Values.database.postgresql.auth.username }}:$(DATABASE_PASSWORD)@{{ include "umami.fullname" . }}-db:{{ .Values.database.postgresql.service.port }}/{{ .Values.database.postgresql.auth.database }}"
            {{- else if $externalDatabaseEnabled }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-db-external" (include "umami.fullname" .)) .Values.database.external.existingSecret }}
                  key: {{ .Values.database.external.databaseUrlKey }}
            {{- end }}
          {{- with .Values.umami.envFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.umami.extraVolumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.umami.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.umami.extraVolumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
